<style>
  .hero-banner__media,
  .hero-banner__image-wrapper,
  .hero-banner .media,
  .hero-media-container {
    background: none !important;
    border: none !important;
    box-shadow: none !important;
    padding: 0 !important;
  }

  .hero-img-contain {
    object-fit: {{ section.settings.image_fit }} !important;
  }
</style>

<section class="relative overflow-hidden bg-gradient-to-br from-surface to-secondary-container/20 pt-16 pb-24 md:pt-24 md:pb-32">
  <div class="max-w-container-max mx-auto px-margin-desktop grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
    <div class="space-y-8">
      <div class="inline-flex items-center gap-3 px-4 py-2 bg-primary/10 text-primary rounded-full font-bold text-sm border border-primary/20">
        <span class="font-bold">{{ section.settings.badge_text }}</span>
      </div>
      <h1 class="font-bold text-[40px] leading-[48px] text-on-surface max-w-xl">
        {{ section.settings.title }}
      </h1>
      <p class="text-lg text-on-surface-variant max-w-lg">
        {{ section.settings.description }}
      </p>
      <div class="flex flex-wrap gap-4">
        <a href="{{ section.settings.button_link }}" class="bg-primary text-on-primary px-8 py-4 rounded-xl font-semibold hover:opacity-90 transition-all flex items-center gap-2 shadow-lg shadow-primary/20">
          {{ section.settings.button_text }}
        </a>
        <a href="{{ section.settings.secondary_button_link }}" class="bg-surface-container-lowest border border-primary text-primary px-8 py-4 rounded-xl font-semibold hover:bg-primary/5 transition-all flex items-center gap-2">
          <span class="material-symbols-outlined">chat</span>
          {{ section.settings.secondary_button_text }}
        </a>
      </div>
    </div>
    <div class="relative flex justify-center items-center">
      <div class="absolute inset-0 w-full h-full bg-primary/5 rounded-full blur-[120px] opacity-30"></div>
      <div class="relative z-10 w-full flex justify-center">
        {%- if section.settings.hero_image != blank -%}
          <div class="hero-media-container relative w-full max-w-[650px] aspect-square md:aspect-video lg:aspect-square flex items-center justify-center p-0" style="min-height: {{ section.settings.banner_height }}px;">
            <img src="{{ section.settings.hero_image | image_url: width: 1400 }}" 
                 alt="{{ section.settings.title }}" 
                 class="w-full h-full hero-img-contain drop-shadow-[0_20px_50px_rgba(0,68,41,0.15)] transition-transform duration-700 hover:scale-105">
          </div>
        {%- else -%}
          <div class="rounded-3xl w-full aspect-square bg-surface-container/50 flex items-center justify-center text-on-surface-variant border-2 border-dashed border-outline/20">
            <span class="material-symbols-outlined text-6xl opacity-10">image</span>
          </div>
        {%- endif -%}
      </div>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Hero Banner",
  "settings": [
    {
      "type": "text",
      "id": "badge_text",
      "label": "Badge Text",
      "default": "TRUSTED BY 10,000+ PHARMACY STUDENTS"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "QUALITY PHARMACY NOTES MADE FOR B.PHARM STUDENTS"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Handwritten exam-focused notes, GPAT materials & instant PDF downloads. Master your curriculum with clarity and speed."
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Primary Button Text",
      "default": "Shop Notes"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Primary Button Link"
    },
    {
      "type": "text",
      "id": "secondary_button_text",
      "label": "Secondary Button Text",
      "default": "Join WhatsApp"
    },
    {
      "type": "url",
      "id": "secondary_button_link",
      "label": "Secondary Button Link"
    },
    {
      "type": "image_picker",
      "id": "hero_image",
      "label": "Hero Image"
    },
    {
      "type": "select",
      "id": "image_fit",
      "label": "Media Fitting",
      "options": [
        { "value": "contain", "label": "Contain (No Cropping)" },
        { "value": "cover", "label": "Cover (Fill Space)" }
      ],
      "default": "contain"
    },
    {
      "type": "range",
      "id": "banner_height",
      "min": 400,
      "max": 800,
      "step": 50,
      "default": 600,
      "unit": "px",
      "label": "Min Section Height"
    }
  ],
  "presets": [
    {
      "name": "Hero Banner"
    }
  ]
}
{% endschema %}
